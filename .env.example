# 前台为静态导出，内容来自仓库内 `data/*.json` 与 `public/posters/`。
# 合并协作者发来的内容包后执行 `npm run build`，再将 `out/` 上传到 TOS + CDN 即可。
#
# 若将来恢复「运行时拉取 JSON」，可再配置：
# NEXT_PUBLIC_CONTENT_API_URL=https://你的 CDN 域名/content.json
